<%= form_for(@dle) do |f| %>
  <% if @dle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dle.errors.count, "error") %> prohibited this dle from being saved:</h2>

      <ul>
      <% @dle.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
